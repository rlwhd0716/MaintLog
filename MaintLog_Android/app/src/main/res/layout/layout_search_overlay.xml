<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/layoutSearchOverlayRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#CCFFFFFF"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingTop="16dp"
    android:paddingEnd="16dp">

    <!-- 검색 입력 영역 -->
    <LinearLayout
        android:id="@+id/layoutSearchInputBox"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:background="@drawable/bg_home_search_bar"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="12dp">

        <ImageView
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:src="@drawable/ic_search"
            app:tint="@color/colorTextSecondary" />

        <EditText
            android:id="@+id/etIntegratedSearch"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:fontFamily="@font/pretendard_medium"
            android:hint="요청사항 / 장애 / 작업번호 / 설비 검색"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:singleLine="true"
            android:textColor="@color/colorTextPrimary"
            android:textColorHint="@color/colorTextSecondary"
            android:textSize="@dimen/content_14" />

        <TextView
            android:id="@+id/btnCloseSearch"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center"
            android:paddingStart="12dp"
            android:paddingEnd="4dp"
            android:text="취소"
            android:textColor="@color/colorPrimary"
            android:textSize="@dimen/content_14" />

    </LinearLayout>

    <!-- 검색 안내 / 결과 영역 -->
    <LinearLayout
        android:id="@+id/layoutSearchContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_search_result_card"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/pretendard_semibold"
            android:text="통합 검색"
            android:textColor="@color/colorTextPrimary"
            android:textSize="@dimen/content_16" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="요청사항, 장애, 작업번호, 설비명을 검색할 수 있습니다."
            android:textColor="@color/colorTextSecondary"
            android:textSize="@dimen/content_14" />

    </LinearLayout>

</LinearLayout>